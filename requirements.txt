# Anti-Piracy Detection Pipeline — Python Dependencies
# Pin versions once the stack is stable; ranges here for initial setup.

# --- Browser automation ---
playwright>=1.44.0          # headless scraping with JS rendering

# --- Google Sheets / Drive integration ---
gspread>=6.0.0              # Google Sheets read/write via service account
google-auth>=2.29.0         # OAuth2 / service account auth for gspread

# --- Fuzzy text matching ---
rapidfuzz>=3.9.0            # token_set_ratio for title similarity

# --- HTTP & parsing ---
httpx>=0.27.0               # async-capable HTTP client for API calls
beautifulsoup4>=4.12.0      # HTML parsing fallback when JSON not available
lxml>=5.2.0                 # fast HTML/XML parser backend for bs4

# --- Data handling ---
pydantic>=2.7.0             # data validation for detection/score dataclasses

# --- Utilities ---
python-dotenv>=1.0.0        # load .env files for local development
structlog>=24.1.0           # structured JSON logging
tenacity>=8.3.0             # retry / backoff decorator for API calls
click>=8.1.0                # CLI argument parsing for individual scripts

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
responses>=0.25.0           # mock httpx responses in tests
